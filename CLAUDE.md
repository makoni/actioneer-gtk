# CLAUDE.md

The instructions for this repository live in `AGENTS.md`, which every agent
here shares. Imported below so Claude Code loads it automatically rather than
waiting for someone to open it.

@AGENTS.md
